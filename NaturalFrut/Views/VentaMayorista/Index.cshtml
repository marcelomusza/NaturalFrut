@model IEnumerable<NaturalFrut.Models.VentaMayorista>
@{
    ViewBag.Title = "VentaMayorista";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Ventas Mayoristas</h2>

<div class="row">
    <div class="col-sm-12">
        <p>
            @Html.ActionLink("Ingresar Venta", "NuevaVentaMayorista", null, new { @class = "btn btn-primary" })
        </p>
    </div>
</div>

<div class="row">
    <div class="col-sm-12">

        @if (!Model.Any())
        {
            <h2>No se han cargado aún ventas mayoristas en el sistema</h2>
        }
        else
        {

            <table id="ventasmayoristas" class="table table-bordered table-hover">
                <thead>
                    <tr>
                        <th>Fecha</th>
                        <th>Acciones</th>
                    </tr>
                </thead>
                <tbody>

                </tbody>
            </table>

        }
    </div>
</div>

@section scripts
{

    <script>

        $(document).ready(function () {

            var table = $("#ventasmayoristas").DataTable({

                "language": {
                    "url": "http://cdn.datatables.net/plug-ins/1.10.16/i18n/Spanish.json"
                },

                ajax: {

                    url: "/api/ventasmayoristas",
                    dataSrc: ""
                },
                columns: [
                    {
                        data: "Fecha"
                    },
                    {
                        data: "ID",
                        render: function (data) {
                            return "<button class='btn-link js-delete' data-ventamayorista-id=" + data + "><img title='Borrar' src='/Content/Images/DeleteIcon.png'/></button>" +
                                "<button class='btn-link js-edit' data-ventamayorista-id=" + data + "><img title='Editar' src='/Content/Images/EditIcon.png'/></button>";
                        }
                    }
                ]
            });

            $("#ventasmayoristas").on("click", ".js-delete", function () {

                var button = $(this);

                bootbox.confirm("¿Estas seguro de realizar esta acción?", function (result) {

                    if (result) {
                        $.ajax({
                            url: "/api/ventasmayoristas/" + button.attr("data-ventamayorista-id"),
                            method: "DELETE",
                            success: function () {

                                toastr.success("Cliente eliminado satisfactoriamente");

                                table.row(button.parents("tr")).remove().draw();
                            }
                        });
                    }

                });


            });

            $("#ventasmayoristas").on("click", ".js-edit", function () {

                var button = $(this);

                location.href = "EditarVentaMayorista/" + button.attr("data-ventamayorista-id");

            });


        });




    </script>

}