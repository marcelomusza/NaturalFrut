@model IEnumerable<NaturalFrut.Models.TipoDeUnidad>
@{
    ViewBag.Title = "TipoDeUnidad";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Tipos de Unidad</h2>

<div class="row">
    <div class="col-sm-12">
        <p>
            @Html.ActionLink("Agregar nuevo", "NuevoTipoDeUnidad", null, new { @class = "btn btn-primary" })
        </p>
    </div>
</div>

<div class="row">
    <div class="col-sm-12">

        @if (!Model.Any())
        {
            <h2>No se han cargado aún Datos en el sistema</h2>
        }
        else
        {

            <table class="table table-bordered table-hover">
                <tr>
                    <th>Nombre</th>
                    <th>Acciones</th>
                </tr>
                <tbody>
                    @foreach (var tipoDeUnidad in Model)
                    {
                        <tr>
                            <td>@tipoDeUnidad.Nombre</td>
                            <td>
                                @Html.ActionLink("Editar", "EditarTipoDeUnidad", "Admin", new { id = tipoDeUnidad.ID }, null)
                                &nbsp;
                                @Html.ActionLink("Borrar", "BorrarTipoDeUnidad", "Admin", new { id = tipoDeUnidad.ID }, null)
                            </td>
                        </tr>
                    }
                </tbody>
            </table>


        }

    </div>
</div>